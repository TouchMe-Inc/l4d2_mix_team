# l4d2_mix_team
Plugin adds voting for mixing teams.

## Commands
To start voting, type `!mix <type>`. Example:

`!mix random` - Random mixing

`!mix capitan` - Mixing by choosing captains (captains recruit teammates in turn)
